# NetScan.py
 NetScan — a single-file Python tool that scans hosts for open ports, fingerprints services and OS, and cross-references detected software versions against known CVEs via the NVD database.
